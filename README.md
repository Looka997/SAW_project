## Database Credentials file

Inside of `/db_connections` create a file named `credentials.php` with the following structure:

```php
<?php
    define ('HOST', "");
    define ('DB_USER', "");
    define ('DB_PASSWORD',"");
    define ('DATABASE', "");
?>
```
